/**
 * BACKWARD COMPATIBILITY — Re-export from controllers.
 * @deprecated Import from '@/controllers/useSearchController' instead.
 */
export { SearchProvider, useSearch } from '@/controllers/useSearchController';
